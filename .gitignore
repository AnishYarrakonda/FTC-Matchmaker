# This repo is PUBLIC. Documentation and images only — never application code.

# environment / secrets (must never appear here)
.env
.env.*
*.key
*.pem
*.p12
secrets.*
credentials.*
*.jwt

# internal docs — audit reports, remediation plans, handoffs
*QA-REPORT*
*QA-FINDINGS*
*REMEDIATION*
*HANDOFF*
/internal/
/private/

# build artifacts / dependencies (nothing should be built here)
node_modules/
.next/
dist/
build/

# os / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
